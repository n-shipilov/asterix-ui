import type { OffsetOptions, Placement, VirtualElement } from "@floating-ui/react";

export type PopupAnchorElement = Element | VirtualElement;
export type PopupPlacement = Placement;
export type PopupOffset = OffsetOptions;
